# TODOs

## Implement

- [DONE] Custom HTTP headers configurable
- [DONE] Extend image blocks so it is not only base64
- [DONE] Document content-block type (PDF for example)
- [DONE] Retry/backoff // "No normalized error taxonomy" -> check both files. Figure out what makes sense and what not to implement. Don't be lazy and just do the less effort choice, actually verify it is the one we should take.
- [DONE] "There is no shared scenario suite run identically across all three backends" -> this is a true gap, we should fix it. We want to make sure somebody using the provider will be able to use the same generic code, just with different models (only specific config per provider, because even if we want it generic yes, they should be able to tune each provider as they want)

## Investigate

- [DROPPED] redacted_thinking, check what this is and if we want to add support to it or not -> legacy-model-only block (classic budget_tokens thinking, not current adaptive thinking); already round-trips fine today through the existing generic block passthrough (`_serialize`'s catch-all + `_deserialize` not transforming content). The "new way" (adaptive thinking + `display`) is already supported via existing `config` passthrough + the existing `ThinkingBlock`. Nothing to add. Caveat: passthrough is a runtime-only guarantee -- it's not a member of the typed `Block` union, so it's not statically type-visible to callers (this library ships type hints). Not a blocker, just a caveat.
- [DONE] Claude pause_turn, wtf is that? Do we want to support it? -> added (trivial, Claude-only StopReason mapping).
- [DONE] Claude prompt-caching usage fields -> covers Claude (cache_creation/cache_read_input_tokens) and OpenAI (usage.input_tokens_details.cached_tokens/cache_write_tokens) both, normalized into vendor-neutral `cache_read_tokens`/`cache_write_tokens`. Also normalizes `input_tokens` itself to mean "total input tokens" across both vendors: Claude's `input_tokens` natively excludes cache tokens while OpenAI's already includes them, so Claude's is now folded with the cache fields to match. Streaming was fixed to carry cache fields too (previously only the non-streaming path did).
- [DEFERRED - revisit later] model-capability probing -> real gap (Ollama's /api/show -> capabilities), but needs its own design pass first: new public API surface (a method only Ollama does anything real with), a caching/TTL strategy per model (avoid N+1 /api/show calls), and a decision on what ducktape-provider does with the info (pre-flight error vs. just expose it). Not a small bolt-on like the others above.
- [DONE] Per-request tunables -> already properly supported on all 3 adapters (`config` merges into the request body after the adapter's own defaults, before headers/timeout are popped -- verified in all three `_build_request`s); no first-class typed fields added on purpose (Ollama nests knobs under `options`, so a "generic" field would still need per-adapter translation, no real gain over passthrough). README now documents the passthrough mechanism with an example instead of leaving it source-diving-only.
- [DROPPED] "Result shape is thinner than planned" -> cost estimate -> no vendor reports price in the response; would mean hand-maintaining our own pricing table forever for a stdlib-only library, and it's trivially computable by the caller from the already-normalized `usage`. Not worth it.
- [DONE] "Result shape is thinner than planned" -> latency -> `latency_ms: float` added to `Response` (always populated, self-measured, not vendor-reported) on all 3 adapters, buffered and streaming paths both. Caveat: streaming's timer starts before request-serialization, buffered's starts after -- so the two numbers aren't directly comparable, not a bug.
- [DEFERRED - revisit later] "Result shape is thinner than planned" -> parsed structured output -> real gap (Claude/OpenAI/Ollama each need a different translation: forced-tool-call trick, `text.format` json_schema, `format` body field), needs its own design pass (schema translation per vendor + validation/error surfacing), same category as model-capability probing above. Not doing now, revisit later.
- [DROPPED] "No config file / no budgets" -> embedded-library goal means the parent project already owns config lifecycle (its own env/settings), an auto-loaded TOML would add filesystem-discovery magic for a problem that doesn't exist; matches the explicit-injection-over-discovery call already made for the adapter registry. Budgets specifically also blocked by the dropped cost-estimate item (no pricing data to enforce against); token-count budgets are an orchestration/caller concern, same bucket as retry/backoff.
- [DONE] "Registry: simpler than planned, not necessarily worse" -> added opt-in `Provider(autodiscover=True)`, off by default: discovers third-party adapters under the `ducktape_provider.adapters` entry-point group via stdlib `importlib.metadata`, merged with `setdefault` so explicit/built-in adapters always win, never overridden. A broken third-party entry point is logged and skipped, not fatal. Documented in README.
- [DONE] " `chat`/`stream_chat` as two methods, not one verb with a flag" -> confirmed correct as-is, nothing to change. A plain `stream: bool` can't give a precise return type (would force `Response | Iterator[StreamEvent]` + isinstance checks everywhere); `@overload` with `Literal[True/False]` only helps when the caller passes a literal, not a runtime variable -- degrades to the same union problem for dynamic use anyway. Two methods also map cleanly onto vendor reality (each backend genuinely has two distinct request modes).
- [DONE] "Sync-only (open question 3 answered by default, not decided)" -> added `Provider.async_chat`/`Provider.async_stream_chat`, thread-pool-backed via stdlib `asyncio.to_thread` (no httpx/aiohttp dependency, adapters stay 100% sync/unchanged), kept on `Provider` itself, no separate async class. Proven non-blocking via a concurrent-counter test, not just a pass-through check.
- [DONE] " **Open question 1 (tools at v0.1 vs v0.2)**: resolved in the more ambitious direction..." -> checked the primary source doc directly. Original question: "full tool-definition support on all backends at v0.1, or chat+structured+embed first and tools in v0.2?" -- deferred meant pushing tools to v0.2 behind chat/structured-output/embed. What happened: tools shipped on all 3 backends immediately at v0.1. Side note: the other two prerequisites (structured output, embed) never shipped either way, so the 3-way bundling is moot beyond tools specifically -- nothing to reconcile, just recorded.
- [DONE] **Streaming event protocol** -> compared all 3 vendors' native streaming shapes against the current `StreamEvent` union: confirmed correct, mirrors Claude's (richest) block-lifecycle model, OpenAI/Ollama adapters properly contain the synthesis complexity needed to fake that lifecycle from their flatter native protocols. One real gap found and fixed in the process: OpenAI's `reasoning` output items (opt-in via `reasoning.summary` config) were silently dropped in both buffered and streaming paths -- now mapped to `ThinkingBlock`/`thinking_delta`, matching Claude's existing thinking support.
